# Mariadb role
This role installs, starts and enables MariaDB server.
It creates a database, users and tables.

## Usage
1) Set variables such as: database, username and password to be created.
2) Include sql queries in files folder in order to create tables and load data into database.
